<Window x:Class="MeerkeuzevragenUI.NieuweVraagWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeerkeuzevragenUI"
        mc:Ignorable="d"
        Title="NieuweVraagWindow" Height="450" Width="460">
    <Grid Margin="0,0,331,-6">
        <StackPanel Margin="15,15,-319,24">

            <TextBlock x:Name="txtGekozenOnderwerp" FontWeight="Bold" FontSize="14" Margin="0,0,0,10" Foreground="Blue"/>

            <TextBlock Text="Vraag:" FontWeight="SemiBold"/>
            <TextBox x:Name="txtVraagTekst" Height="50" TextWrapping="Wrap" Margin="0,5,0,10" AcceptsReturn="True" />

            <TextBlock Text="Moeilijkheidsgraad:" FontWeight="SemiBold"/>
            <ComboBox x:Name="cmbMoeilijkheid" Margin="0,5,0,15" Width="150" HorizontalAlignment="Left"/>

            <TextBlock Text="Antwoorden (Vink het juiste antwoord aan):" FontWeight="SemiBold" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <RadioButton x:Name="rbCorrect1" GroupName="CorrectAntwoord" IsChecked="True" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtAntwoord1" Width="400" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <RadioButton x:Name="rbCorrect2" GroupName="CorrectAntwoord" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtAntwoord2" Width="400" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <RadioButton x:Name="rbCorrect3" GroupName="CorrectAntwoord" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtAntwoord3" Width="400" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <RadioButton x:Name="rbCorrect4" GroupName="CorrectAntwoord" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtAntwoord4" Width="400" />
            </StackPanel>

            <Button Content="Vraag Opslaan" Height="35" Width="120" HorizontalAlignment="Right" Click="BtnOpslaan_Click" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="MeerkeuzevragenUI.ResultatenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeerkeuzevragenUI"
        mc:Ignorable="d"
        Title="ResultatenWindow" Height="400" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtTitel" Text="Overzicht Resultaten" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <DataGrid x:Name="dgResultaten" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student" Binding="{Binding Eigenaar.Naam}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Toets / Onderwerp" Binding="{Binding AfgelegdeToets.Onderwerp.Naam}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Behaalde Score" Binding="{Binding Score}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Max. Score" Binding="{Binding MaxScore}" Width="100" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>

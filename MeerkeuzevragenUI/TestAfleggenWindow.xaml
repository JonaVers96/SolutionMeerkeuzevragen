<Window x:Class="MeerkeuzevragenUI.TestAfleggenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test Afleggen" Height="600" Width="800" WindowStartupLocation="CenterOwner" Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtTitel" FontSize="22" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icVragen">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="{Binding DeVraag.VraagTekst}" FontWeight="SemiBold" FontSize="16" TextWrapping="Wrap" Margin="0,0,0,10"/>

                                <ListBox ItemsSource="{Binding DeVraag.Antwoorden}" SelectedItem="{Binding GekozenAntwoord}" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <RadioButton Content="{Binding Tekst}" 
                                                         IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" 
                                                         Margin="5,3,0,3" FontSize="14"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>

                                <TextBlock Text="{Binding FeedbackTekst}" Foreground="{Binding FeedbackKleur}" FontWeight="Bold" Margin="0,10,0,0" FontSize="14" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Test Indienen" Click="BtnIndienen_Click" Height="40" Margin="0,15,0,0" Background="#2196F3" Foreground="White" FontWeight="Bold" FontSize="16"/>
    </Grid>
</Window>